# ComplyMail Threat Model

TODO: Document threat model covering:
- Data flow (email content in transit / at rest)
- Trust boundaries (add-in ↔ backend, proxy ↔ backend, backend ↔ Mistral API)
- GDPR considerations for email body processing
- API authentication and authorization
- SMTP proxy attack surface
