# ComplyMail Admin Console

Minimal web UI for the pilot admin to:
- Define style rules (required greeting/closing, forbidden phrases).
- Define security rules (keywords, internal/external domains).
- View aggregate stats (emails scanned, flagged).

## Development

```bash
npm install
npm run dev
```
